# GLM / Zcode implementation prompt — Serah Terima / Stock Entry (handover)

Copy the text below into a fresh Zcode session with this project open. Sending it authorizes implementation of the Serah Terima / Stock Entry scope (tasks T21–T26); merely reading this file does not.

---

Implement the Serah Terima / Stock Entry handover (Cold Storage Produksi → Gudang Barang Jadi) in `/Users/rotiropi/erpnext-new/apps/production_app`.

First read `AGENTS.md`, `HANDOVER_PLAN.md`, `TASKS.md` (section E), and `PROJECT_STATE.md`, in that order. `HANDOVER_PLAN.md` owns this scope's business rules; `TASKS.md` defines task dependencies and acceptance; `PROJECT_STATE.md` records actual work and evidence (the Work Order scope T01–T20 in it is COMPLETE — do not redo it). Later explicit user corrections take precedence; update the documents when a correction changes them.

Use the existing mockup at `/Users/rotiropi/mockup_production_app` as the UI source — the page is `src/HandoverBoard.vue` (4-lane kanban "Stock Entry": Cold Storage → Request Gudang → Siap Kirim → Terkirim) plus its dialogs; quantity input is `src/QtyInput.vue`. Do not redesign it or edit that source repository. The SPA to extend is `workspace_frontend/` in the app (the Work Order workspace, route-based hash navigation); add the handover page there, do not create a second frontend or backend.

Confirmed user decisions — do not ask these again: approach is full-native mapping with **no new business DocType**; request = native Material Request (type Material Transfer) + custom fields; lot = the finished-goods Batch in the Cold Storage warehouse (remaining read from the live ledger); one new Role **"Gudang Barang Jadi"** (creates/cancels Material Requests, sees only the Stock Entry page in this app) plus the existing **Manufacturing User** as the production side (post-packing + send); Box 1/2 in the request dialog are **text box identifiers** (Data fields on the MR — unrelated to `custom_box_1/2` kg on Work Order); after sending with good < requested the MR is **stopped (short-closed) natively**; cancelling a request is a **native MR cancel**; the target warehouse is a **fifth default on Manufacturing Settings** edited via the existing "Pengaturan" menu — never hardcode a warehouse name; Good Post-Packing is the only quantity that moves stock; Reject/Trial/Sisa are notes only; requested/pre-/post-packing quantities are never forced equal.

Verified during planning on 2026-09-13 (re-verify at T01-equivalent before relying on it): warehouses `Cold Storage Produksi - ROPI` and `Gudang Barang Jadi - ROPI` exist; Stock Entry DocPerm already grants Manufacturing User full access, but **Material Request DocPerm grants Manufacturing User nothing** (only Purchase/Stock roles) — both permission additions are custom DocPerms created idempotently in `upgrade.py` with a pre-change snapshot; MR custom fields today are only `custom_note`; the MR→SE builder is `material_request.py:936 make_stock_entry` (Material Transfer maps `s_warehouse = item.from_warehouse`, `t_warehouse = item.warehouse`); batch quantity by warehouse is `batch.py:236 get_batch_qty`; Work Order already has the `custom_*_postpacking` / `custom_jam_packing` / `custom_qc_packing` mirror fields.

Start from T21 (prove the native MR→SE path on isolated test records). One active task at a time: set it IN_PROGRESS in `PROJECT_STATE.md` before editing, complete its acceptance check, record real evidence, then continue without asking permission at every task boundary. Do not build UI before the transaction proof (T21) and server actions (T22–T24) pass their gates.

Before every final response, interruption handoff, or completed/blocked task, update `PROJECT_STATE.md` with changes, actual commands/results, blockers, and a precise next step. If resuming after context loss, read that file and inspect current git changes; do not redo completed work. Never mark DONE without the required verification; a skipped test is NOT RUN, never PASS. Reuse the existing test suite and locking/permission patterns from `production_app/api/work_order.py` and `production_app/tests/` rather than inventing parallel ones.

Runtime facts (verify drift at start): backend Docker container `erpnext-new-backend-1`, bench `/home/frappe/frappe-bench`, site `frontend`, UI `http://localhost:8081`; the container image owns `apps/`, so host→container sync is `docker cp` + backend restart (then `docker exec -u root ... chown frappe:frappe` on copied files), per the recorded T01/T13 procedure; frontend builds run inside the container and built assets must be copied to `sites/assets` in **both** backend and frontend containers with the regenerated inline HTML; keep `server_script_enabled=1` in common_site_config (batch-item WO submission breaks site-wide without it). Create dedicated non-admin test users (gudang and produksi roles) for permission and browser checks and delete them with all test documents when done.

Use available Zcode terminal/browser tools, not Codex-specific tool names. Use CodeGraph before text search only where a `.codegraph/` index exists; if unavailable, record that and search normally. Report to the user in Indonesian; keep repository technical documentation in English, consistent with existing files.

Reuse ERPNext native document methods and preserve the `bakery_manufacturing` batch override untouched. Do not edit ERPNext/Frappe core, bypass permissions (`ignore_permissions`), force statuses, duplicate batch logic, create a second stock ledger, commit, push, or deploy. Protect operational data; back up specific definitions before any cutover; leave unrelated scripts alone. If blocked, record the concrete error and evidence in `PROJECT_STATE.md`, stop dependent work, and ask only for decisions outside the approved scope.

Stop after T26 passes with its acceptance matrix executed. Further scopes (e.g. undo after Terkirim, multi-WO requests) are separate requests.
